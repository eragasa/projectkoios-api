# projectkoios-api

This repo owns the FastAPI HTTP interface.

Routing and role split live in `projectkoios-bootstrap/docs/agent-charter.md`.
This repo does not own cross-repo routing or product architecture policy.
